# 🔧 FRONTEND ENVIRONMENT CONFIGURATION
# Copy this file to .env and fill in your values

# Backend API URL
# MUST match your backend server address
# Both machines should use the SAME value for consistency
REACT_APP_API_URL=http://localhost:5000/api

# Alternative format (if not using /api suffix in backend):
# REACT_APP_API_BASE=http://localhost:5000

# For Production:
# REACT_APP_API_URL=https://your-backend-domain.com/api

# ====================================================================
# IMPORTANT NOTES FOR FIXING IMAGE DISPLAY ISSUES:
# ====================================================================
# 
# 1. BOTH MACHINES must use the SAME backend URL
#    - If backend is on localhost:5000, BOTH use localhost:5000
#    - If backend is on 192.168.1.10:5000, BOTH use that IP
# 
# 2. If images are stored as Base64 in MongoDB:
#    - Images are IN the database
#    - Both machines will see images automatically
#    - No need to copy image files
# 
# 3. If images are stored as files:
#    - Both machines need access to the SAME files
#    - Use cloud storage (Cloudinary) or shared network drive
#    - Or use Base64 to store images in database
# 
# 4. After changing .env, RESTART the React development server:
#    - Stop: Ctrl+C
#    - Start: npm start
# 
# 5. Clear browser cache if images still don't show:
#    - Hard refresh: Ctrl+Shift+R (Windows) or Cmd+Shift+R (Mac)
# 
# ====================================================================

# Stripe Public Key (Optional - for payments)
REACT_APP_STRIPE_PUBLIC_KEY=your_stripe_publishable_key

# Google Analytics (Optional)
REACT_APP_GA_TRACKING_ID=UA-XXXXXXXXX-X

# Feature Flags (Optional)
REACT_APP_ENABLE_CART=true
REACT_APP_ENABLE_CUSTOM_BOX=true
REACT_APP_ENABLE_PAYMENTS=true

# Debug Mode
REACT_APP_DEBUG=false
