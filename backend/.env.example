# 🔧 BACKEND ENVIRONMENT CONFIGURATION
# Copy this file to .env and fill in your values

# Server Configuration
PORT=5000
NODE_ENV=development

# MongoDB Configuration
# Local MongoDB:
MONGODB_URI=mongodb://localhost:27017/dinujaya-flora

# OR MongoDB Atlas (Cloud):
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/dinujaya-flora?retryWrites=true&w=majority

# JWT Secret (MUST be at least 32 characters for security)
JWT_SECRET=your_super_secret_jwt_key_at_least_32_characters_long_change_this_in_production

# Frontend URLs (for CORS)
CLIENT_URL=http://localhost:3000
FRONTEND_URL=http://localhost:3000

# Cloudinary Configuration (Optional - for cloud image storage)
CLOUDINARY_CLOUD_NAME=your_cloudinary_cloud_name
CLOUDINARY_API_KEY=your_cloudinary_api_key
CLOUDINARY_API_SECRET=your_cloudinary_api_secret

# Email Configuration (Optional - for notifications)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your_email@gmail.com
EMAIL_PASS=your_app_password

# Stripe/Payment Gateway (Optional - for payments)
STRIPE_SECRET_KEY=your_stripe_secret_key
STRIPE_WEBHOOK_SECRET=your_stripe_webhook_secret

# Admin Default Credentials (for first setup)
ADMIN_EMAIL=admin@dinujayaflora.com
ADMIN_PASSWORD=admin123
ADMIN_NAME=Admin User

# ====================================================================
# IMPORTANT NOTES:
# ====================================================================
# 
# 1. NEVER commit .env file to Git
# 2. Each developer should have their own .env file
# 3. Both machines MUST use the same MONGODB_URI to see same data
# 4. JWT_SECRET must be the same on all machines
# 5. If using Base64 images, they're stored IN the database,
#    so both machines will see the same images automatically
# 6. Make sure MongoDB is running before starting the server
# 
# ====================================================================

# File Upload Limits
# These are handled in server.js with express.json({ limit: '50mb' })
# Current limit: 50MB for Base64 images
